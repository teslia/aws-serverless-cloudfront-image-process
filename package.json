{
  "name": "image-process-lambda",
  "version": "1.0.0",
  "description": "AWS cloudfront image process lambda@edge",
  "license": "ISC",
  "scripts": {
    "install-local":"rm -rf node_modules && npm install",
    "install-lambda":"rm -rf node_modules && npm install --platform=linux --arch=x64",
    "deploy":"serverless deploy"
  },
  "dependencies": {
    "aws-sdk": "^2.1238.0",
    "sharp": "^0.31.1"
  },
  "devDependencies": {
    "serverless": "^3.23.0",
    "serverless-plugin-existing-cloudfront-lambda-edge": "^2.0.1"
  }
}
